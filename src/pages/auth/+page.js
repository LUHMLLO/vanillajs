import Page from '~lib/packages/pager/@';

export const page = new Page();

page.SEO({
	icon: '',
	author: 'lm',
	description: 'Vanilla JS - Auth Page',
	image: '',
	keywords: [''],
	title: 'Auth',
});

let js = String.raw;
page.JS(js``);

let html = String.raw;
page.HTML(html`
    
`);

let css = String.raw;
page.CSS(css``);
